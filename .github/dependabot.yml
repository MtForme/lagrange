version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly

  - package-ecosystem: pip
    directory: /
    schedule:
      interval: monthly
    # Library lower bounds only — group the noise into one PR.
    groups:
      python-deps:
        patterns: ["*"]
